# Credenciais e config local
.clasp.json
credentials.json
token.json
*.env

# Sistema operacional
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Impeccable: config de exceções é versionada; cache de sessão não
.impeccable/hook.cache.json

# Dados pessoais da equipe (nome, e-mail, cargo) — nunca versionar
Equipe*.xlsx
